import { NextResponse } from "next/server";

export async function GET() {
  // This is a placeholder endpoint.
  // We will add more logic to log errors.
  return NextResponse.json({ message: "Auth error logging endpoint." });
}
